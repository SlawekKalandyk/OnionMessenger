import { MessageTimestampSortPipe } from './message-timestamp-sort.pipe';

describe('MessageTimestampSortPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageTimestampSortPipe();
    expect(pipe).toBeTruthy();
  });
});
